{
  "name": "swissygo-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SwissYGO overhaul — Fastify + SQLite backend (accounts, self-registration, live pairing, history)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "@fastify/jwt": "^9.0.1",
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.7.0",
    "fastify": "^5.2.0"
  }
}
