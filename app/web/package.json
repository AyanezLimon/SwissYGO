{
  "name": "swissygo-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SwissYGO overhaul — plain static SPA (no build step). npm is dev-only (tests).",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
